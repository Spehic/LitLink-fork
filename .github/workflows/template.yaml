name: CI/CD for LitLink

on:
  push:
    branches:
      - main

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
    # - uses: actions/checkout@v4
    # - name: Set up JDK 17
    #   uses: actions/setup-java@v4
    #   with:
    #     java-version: '17'
    #     distribution: 'temurin'
    #     cache: maven

    # - name: Build with Maven
    #   run: mvn clean package --file backend/pom.xml

    # - name: Set up Docker Buildx
    #   uses: docker/setup-buildx-action@v3

    # - name: Log in to GitHub container registry
    #   uses: docker/login-action@v3
    #   with:
    #     registry: ghcr.io
    #     username: ${{ github.actor }}
    #     password: ${{ secrets.GIT_IMG_REG_TOKEN }}

    # - name: Build and push - users
    #   uses: docker/build-push-action@v6
    #   with:
    #     push: true
    #     tags: ghcr.io/spehic/users:latest
    #     context: backend/users/

    # - name: Build and push - books
    #   uses: docker/build-push-action@v6
    #   with:
    #     push: true
    #     tags: ghcr.io/spehic/books:latest
    #     context: backend/books/

    # - name: Build and push - lists
    #   uses: docker/build-push-action@v6
    #   with:
    #     push: true
    #     tags: ghcr.io/spehic/lists:latest
    #     context: backend/lists/

    # - name: Build and push - ratings
    #   uses: docker/build-push-action@v6
    #   with:
    #     push: true
    #     tags: ghcr.io/spehic/ratings:latest
    #     context: backend/ratings/

    - name: Configure kubeconfig
      run: |
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > kubeconfig
          export KUBECONFIG=kubeconfig

    - name: Deploy to AKS
        uses: Azure/k8s-deploy@v4
        with:
          namespace: litlink
          action: deploy
          manifests: |
            k8s/users-deployment.yaml
            k8s/books-deployment.yaml
            k8s/lists-deployment.yaml
            k8s/ratings-deployment.yaml
          images: |
            ghcr.io/spehic/users:latest
            ghcr.io/spehic/books:latest
            ghcr.io/spehic/lists:latest
            ghcr.io/spehic/ratings:latest

